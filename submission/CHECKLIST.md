# Submission checklist

Agents for Humans Hackathon (Amazon, on Devpost), Everyday Agents track. Statuses as of 13 September 2026.

Deadline: Monday 14 September 2026, 5:00 pm PDT.

**Deadline in your time zone: Tuesday 15 September 2026, 01:00 West Africa Time.** Aim to press submit by 22:00 WAT on the 14th, so a slow upload or a broken link still has time to be fixed.

Status is one of: done, to do, blocked on (the thing it waits for).

## 1. Accounts

| Item | Owner | Status | Exactly what to do |
|---|---|---|---|
| Devpost account and Join Hackathon | You | to do | Sign in at devpost.com. Open https://agentsforhumans.devpost.com, press Join Hackathon and accept the rules. Start the project entry and save a draft today, so the deadline is not the first save. |
| AWS Builder ID | You | to do | Create it at https://builder.aws.com (sign in, then create a Builder ID). It is separate from your AWS account and needs no card. Write down the email and alias it shows; the submission form asks for the Builder ID. Same item as B6 in `FOUNDER-CHECKLIST.md`. |
| builder.aws profile for posting | You | blocked on Builder ID | Sign in to https://builder.aws.com with the Builder ID. Set your display name to Patrick Oghenejivwe and choose an alias, so the three posts publish under your name. |

## 2. Form fields

| Item | Owner | Status | Exactly what to do |
|---|---|---|---|
| Project name | You | to do | `Second` |
| Tagline | You | to do | Paste the Tagline line from `SUBMISSION.md`: `Say your goals out loud. Second plans them into your calendar, checks each morning what really happened, fixes what it can and hands you the smallest piece.` |
| Track | You | to do | Select Everyday Agents. |
| Repository URL | You | done (public, checked 13 Sep) | Paste `https://github.com/oghenejivwe/second` |
| Licence | You | done | Select MIT. `LICENSE` is at the repository root and GitHub detects it as MIT. |
| Text description | CTO writes, you paste | to do (draft at `SUBMISSION.md`, repository root) | Paste it section by section into the matching boxes, in the file's order. Do not trim the status lines (AgentCore not deployed, AWS table and bucket not created, no real calendar or inbox read, demo runs on data labelled `fixtures`) or the build disclosure (one founder, 10 to 14 Sep 2026, Claude Code as coding assistant, no pre-existing project code, open-source libraries only). The rules require both. |
| Built with | You | to do | Enter these tags: python, strands-agents, pydantic, fastapi, amazon-dynamodb, amazon-s3, amazon-transcribe, aws-secrets-manager, amazon-eventbridge-scheduler, aws-lambda, amazon-bedrock-agentcore, google-gemini, google-calendar-api, gmail-api, tavily, react, typescript, vite, react-flow, zustand, pytest, moto, uv. Add vercel once the demo is live. Leave out Amazon Bedrock (it refused Anthropic models for this account, so no run used it) and Groq (forced tool choice not verified). |
| Architecture diagram | You upload | done: `docs/architecture.png` (3200x2000, labels checked legible on 13 Sep) | Upload `docs/architecture.png` to the form's image gallery, or to the diagram field if the form has one. |
| Video URL | You | blocked on recording the video | Paste the YouTube or Vimeo URL after the video is set to Public. Checks are in section 5. |
| Builder ID | You | blocked on Builder ID | Enter the Builder ID from section 1 exactly as the form asks for it. |
| Testing access link | You | blocked on the Vercel deploy | Paste the Vercel production URL from section 3 into the Try it out field. Add the repository URL as a second link. |

## 3. Testing access

The rules require a link judges can open. The link is a static build of the web app running on example data generated by the real graph code, labelled `fixtures` on screen. It has no API behind it.

| Item | Owner | Status | Exactly what to do |
|---|---|---|---|
| Confirm `web/` is ready to ship | CTO | blocked on the workflow still editing `web/src` | When that workflow finishes: commit `web/`, run `npm run build:demo` inside `web/`, and tell you it passed. The Vercel CLI uploads your local folder, so deploying earlier would ship half-edited files. |
| Deploy the demo build to Vercel | You | blocked on the row above | Run the commands below. Only you can, because they need your Vercel login. `web/vercel.json` already sets the framework (Vite), install (`npm ci`), build (`npm run build:demo`) and output (`dist`). When the CLI asks whether to change those settings, answer no. |
| Open the link logged out | You | blocked on the deploy | In a private window, open the production domain shown under Domains in the Vercel dashboard (the short `something.vercel.app` one, not the long per-deployment URL). If it asks you to log in to Vercel, turn off Vercel Authentication in Project Settings, Deployment Protection. Check the rail shows `fixtures` and that Record, Living Graph, Today, Schedule, Memory and Goals all open. |
| Put the link in the form | You | blocked on the deploy | Try it out field, first link. |
| Name the link in the README | CTO | blocked on the deploy | The README status table says the web app is "Not hosted yet" and the Demo build section says "it is not deployed yet". Replace both with the live URL and push. |
| Local run steps for judges who clone | CTO | done | Already in the README under "Testing it as a judge". Copied below so the form description can point to them. |

Deploy, from the repository root:

```bash
cd web
npx vercel login
npx vercel --prod
```

On the first run the CLI asks to set up and link a project. Accept, create a new project, and say the code is in `./`. If the build log fails on the Node version, set Node.js Version to 24.x in Project Settings and redeploy.

Local run steps (from the README):

```bash
# Web app on example data (Node 24), no accounts needed
cd web
npm ci
npm run dev

# Offline test suite (Python 3.12 and uv), no credentials needed
uv sync
uv run pytest -q

# Live Daily run: set SECOND_MODEL_PROVIDER and that provider's key in .env (see .env.example)
uv run python scripts/live_daily.py
```

## 4. Bonus: builder.aws posts

Each post publicly posted on builder.aws is worth 0.2 points, up to 0.6 for all three. Every title must contain "Agents for Humans"; all three drafts already do. Publish before 14 Sep 5:00 pm PDT (01:00 WAT on the 15th).

| Item | Owner | Status | Exactly what to do |
|---|---|---|---|
| Final read of the drafts | CTO | done (13 Sep) | Read all three against the code and history. Removed one claim nothing in the repository supports (Amazon Nova quotas) and corrected one wrong one (the day "returns no card"). The not-deployed status is stated in each draft. |
| Post 1: `submission/blog/01-agents-for-humans-second.md` | You | blocked on builder.aws profile | On builder.aws, create a new post. Title: the file's first line without the `#`. Paste the rest as the body. Check that headings and code blocks survived, then publish publicly. |
| Post 2: `submission/blog/02-agents-for-humans-structured-output.md` | You | blocked on builder.aws profile | Same steps. |
| Post 3: `submission/blog/03-agents-for-humans-guardrails.md` | You | blocked on builder.aws profile | Same steps. |
| Add each URL to the submission | You | blocked on publishing | Open each post in a private window to confirm it is public. Paste all three URLs into the form's blog or bonus content field; if the form has none, add them as extra links next to Try it out. |

## 5. Before pressing submit

| Item | Owner | Status | Exactly what to do |
|---|---|---|---|
| Every link opens logged out | You | to do | In a private window, open: the repository, the Vercel link, the video, the three builder.aws posts, and https://oghenejivwe.github.io/second/ with `/privacy.html` and `/terms.html`. The repository and the three Pages links returned 200 on 13 Sep. |
| Video is public and at most 5 minutes | You | blocked on recording the video | On YouTube, visibility is Public (Unlisted and Private do not count). On Vimeo, anyone can view. Length is 5:00 or less. It shows the working project, and pitches the problem, who it is for and why it matters. |
| Description claims nothing the demo cannot show | CTO checks, you confirm | to do (`SUBMISSION.md`, repository root) | The rules say the project must work as the video and description show it. Compare each claim with the demo and the code. Watch for: Strands session management (not used), AgentCore (written, not deployed), real calendar or inbox (never read), Groq (not verified), the test count (say "more than 700", no exact number). |
| Submission files are public and the README renders | CTO | to do (pushed 13 Sep; logged-out check left) | Pushed on 13 Sep: `README.md`, `SUBMISSION.md`, `docs/architecture.svg`, `docs/architecture.png` and `submission/`. Open https://github.com/oghenejivwe/second logged out and confirm: the architecture image shows under Architecture, both Mermaid diagrams draw as diagrams and not as code, the least-privilege table renders, and GitHub still shows the MIT licence in the sidebar. |
| Video and description agree | You | blocked on the video | Nothing in the video should appear to read a real calendar or inbox. If a screen shows `fixtures`, leave it visible. |
